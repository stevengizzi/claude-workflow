<!-- workflow-version: 1.1.0 -->
<!-- last-updated: 2026-03-14 -->
# Template: Work Journal Close-Out

> **Autonomous runner mode:** The Work Journal (or its equivalent logs) produces
> a close-out following this template. The runner programmatically populates the
> doc-sync prompt from this structured data.
>
> **Human-in-the-loop mode:** The Work Journal should produce a **filled-in
> doc-sync prompt** (from `templates/doc-sync-automation-prompt.md`) with this
> close-out data embedded directly — skipping this intermediate artifact. This
> template still defines the *content* the Work Journal must track; the delivery
> format changes from standalone block to embedded section within the doc-sync
> prompt.
>
> In both modes, the close-out data prevents the #1 doc-sync failure mode: DEF
> number collisions between the Work Journal's assignments and the doc-sync
> session's independent assignments.

---

# Sprint {SPRINT} — Work Journal Close-Out

> Generated by the Sprint {SPRINT} Work Journal conversation on {DATE}.
> Paste this into the doc-sync prompt so the doc-sync session has full
> visibility into what was tracked, assigned, and resolved during the sprint.

---

## Sprint Summary

- **Sprint:** {SPRINT} — {SPRINT_GOAL}
- **Sessions:** {SESSION_LIST}
- **Tests:** pytest {BEFORE_PYTEST} → {AFTER_PYTEST} (+{DELTA_PYTEST}), Vitest {BEFORE_VITEST} → {AFTER_VITEST} (+{DELTA_VITEST})
- **Review verdicts:** {VERDICT_SUMMARY}

---

## DEF Numbers Assigned During Sprint

> List ALL DEF numbers assigned by this Work Journal, including resolved ones.
> Mark each as OPEN, RESOLVED, or already added to CLAUDE.md.
> The doc-sync session uses this as the canonical DEF assignment list.

| DEF # | Description | Status | Source |
|-------|-------------|--------|--------|
| DEF-{N} | {description} | {OPEN / ✅ RESOLVED / ✅ Added to CLAUDE.md} | {session} |

**Important:** Any DEF marked RESOLVED must NOT appear as an open entry in CLAUDE.md.

---

## DEC Numbers Tracked

> List all DEC entries identified during the sprint. The doc-sync session
> creates the full DEC entries in decision-log.md from session close-outs.

| DEC # | Description | Session |
|-------|-------------|---------|
| DEC-{N} | {description} | {session} |

---

## Resolved Items (do NOT create new DEF entries for these)

> Items that were identified as carry-forwards but resolved within the sprint.
> The doc-sync session must NOT create DEF entries for these.

| Item | Resolution | Session |
|------|------------|---------|
| {description} | {how it was resolved} | {session} |

---

## Outstanding Code-Level Items (not assigned DEF numbers)

> Minor housekeeping items that should be documented in Known Issues but don't
> need dedicated DEF entries unless they persist past the next sprint.

| Item | Location | Severity | Source |
|------|----------|----------|--------|
| {description} | {file path} | {LOW/MEDIUM/INFO} | {session} |

---

## Corrections Needed in Initial Doc-Sync Patch

> If the doc-sync runs before the Work Journal close-out is available (e.g.,
> automated runner), list corrections here. If the Work Journal close-out was
> provided to the doc-sync prompt, this section should say "None — Work
> Journal handoff was included in doc-sync prompt."

{corrections or "None — Work Journal handoff was included in doc-sync prompt."}
