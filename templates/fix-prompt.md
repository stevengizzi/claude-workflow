<!-- workflow-version: 1.0.0 -->
<!-- last-updated: 2026-03-12 -->
# Template: Targeted Fix Prompt

This template is populated by the runner when Tier 2.5 triage recommends
a fix session. Placeholders in `{CURLY_BRACES}` are replaced at runtime.

---

    # Sprint {SPRINT}, Fix Session {FIX_ID}: {DESCRIPTION}

    ## Context
    This is an auto-inserted fix session generated by Tier 2.5 triage.

    **Original session:** {SOURCE_SESSION}
    **Issue type:** {CATEGORY} ({SOURCE_TYPE})
    **Issue description:** {ISSUE_DESCRIPTION}
    **Blocks sessions:** {BLOCKED_SESSIONS}

    ## Pre-Flight Checks
    Before making any changes:
    1. Run the test suite: `python -m pytest --tb=short -q && cd ui && npx vitest run && cd ..`
       Expected: {EXPECTED_TESTS} tests, all passing
    2. Verify git is clean: `git status`
    3. Read the affected file(s) to understand current state:
       {AFFECTED_FILES_READ_LIST}

    ## Objective
    {FIX_DESCRIPTION}

    This is a targeted fix, not a feature session. Keep changes minimal and
    focused on resolving the specific issue described above.

    ## Requirements
    {FIX_REQUIREMENTS}

    ## Constraints
    - Do NOT modify any files outside the scope of this fix
    - Do NOT modify: {DO_NOT_MODIFY_LIST}
    - Do NOT refactor or improve code beyond the specific fix
    - Do NOT add features, optimizations, or enhancements
    - Keep the change surface as small as possible

    ## Affected Files
    {AFFECTED_FILES}

    ## Test Targets
    After implementation:
    - Existing tests: all must still pass
    - New tests to write: regression test for the specific fix
    - Minimum new test count: 1
    - Test command: `python -m pytest --tb=short -q && cd ui && npx vitest run && cd ..`

    ## Definition of Done
    - [ ] Fix implemented as described
    - [ ] All existing tests pass
    - [ ] Regression test written and passing
    - [ ] No unrelated changes made
    - [ ] Change is minimal and focused

    ## Regression Checklist (Session-Specific)
    | Check | How to Verify |
    |-------|---------------|
    | Fix addresses the stated issue | {FIX_VERIFICATION} |
    | No unrelated files modified | `git diff --name-only` shows only affected files |
    | All tests pass | pytest + vitest both pass |

    ## Close-Out
    After all work is complete, follow the close-out skill in
    .claude/skills/close-out.md.

    The close-out report MUST include the structured JSON appendix.

    ## Sprint-Level Regression Checklist (for Tier 2 reviewer)
    {SPRINT_REGRESSION_CHECKLIST}

    ## Sprint-Level Escalation Criteria (for Tier 2 reviewer)
    {SPRINT_ESCALATION_CRITERIA}
